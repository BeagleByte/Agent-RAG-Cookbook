### Setup here is:
- Ollama on host already installed
- Docker container which contains the OpenCode program access the host ollama over host config
- config.json for opencode uses ollama ministral-3:8b
